{
  "name": " (Default)",
  "build": { "dockerfile": "../Dockerfile", "context": ".." },
  "extends": "./base.json",

  // This default config doesn't require a GPU
  "hostRequirements": {
    "gpu": "optional"
  }
}